export default {
   exit_interface_assignments(parsed_rule) {
      console.log("exit_interface_assignments");

      //   parsed_rule.tosca = parsed_rule.value;
   },

   exit_interface_assignments(parsed_rule) {
      console.log("exit_interface_assignments");
   },
};
