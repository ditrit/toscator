tosca_definitions_version: tosca_simple_yaml_1_3

node_types:
  ServerNode:
    properties:
      notification_port:
        type: integer
        constraints:
          - greater_or_equal: 1024

topology_template:
  node_templates:
    my_server:
      type: ServerNode
      properties:
        notification_port: 80
