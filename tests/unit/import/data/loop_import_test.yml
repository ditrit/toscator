tosca_definitions_version: tosca_simple_yaml_1_3

imports:
  - ./loop_child.yml

node_types:
  Compute_loop:
    derived_from: tosca.nodes.Root
    description: tagada
    version: "1.0"
    metadata:
      liunx: love
